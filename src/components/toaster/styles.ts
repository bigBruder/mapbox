import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    top: 150,
    width: "100%",
    fontSize: 10,
  },
  message: {
    fontSize: 14,
    flex: 1,
    flexWrap: "wrap",
  },
});

export default styles;
